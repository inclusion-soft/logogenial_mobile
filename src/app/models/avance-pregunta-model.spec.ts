import { AvancePreguntaModel } from './avance-pregunta-model';

describe('AvancePreguntaModel', () => {
  it('should create an instance', () => {
    expect(new AvancePreguntaModel()).toBeTruthy();
  });
});
