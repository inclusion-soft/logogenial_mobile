import { ResultadoPreguntaModel } from './resultado-pregunta-model';

describe('ResultadoPreguntaModel', () => {
  it('should create an instance', () => {
    expect(new ResultadoPreguntaModel()).toBeTruthy();
  });
});
